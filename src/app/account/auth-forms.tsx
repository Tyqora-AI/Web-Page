"use client";
import { FormEvent, useState } from "react";
import { useRouter, useSearchParams } from "next/navigation";

export function AuthForms(){const [tab,setTab]=useState<"signin"|"register">("signin");const [error,setError]=useState("");const [busy,setBusy]=useState(false);const router=useRouter();const search=useSearchParams();
 async function submit(e:FormEvent<HTMLFormElement>,kind:"login"|"register"){e.preventDefault();setBusy(true);setError("");const form=e.currentTarget;const body=Object.fromEntries(new FormData(form));if(kind==="register"&&body.password!==body.confirmPassword){setBusy(false);setError("Passwords do not match.");return}const res=await fetch(`/api/auth/${kind}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const data=await res.json();setBusy(false);if(!res.ok){setError(data.error||"Please try again.");return}router.push(search.get("next")||"/portal");router.refresh()}
 return <div className="auth-panel"><div className="auth-tabs"><button className={tab==="signin"?"active":""} onClick={()=>{setTab("signin");setError("")}}>Sign in</button><button className={tab==="register"?"active":""} onClick={()=>{setTab("register");setError("")}}>Create account</button></div>{tab==="signin"?<form className="auth-form" onSubmit={e=>submit(e,"login")}><div><h2>Welcome back</h2><p>Enter your details to access the portal.</p></div><label><span>Email address</span><input name="email" type="email" autoComplete="email" required/></label><label><span>Password</span><input name="password" type="password" autoComplete="current-password" required/></label>{error&&<p className="form-error" role="alert">{error}</p>}<button className="button button-primary button-large full-button" disabled={busy}>{busy?"Signing in…":"Sign in →"}</button></form>:<form className="auth-form" onSubmit={e=>submit(e,"register")}><div><h2>Create an account</h2><p>For healthcare professionals and partner organisations.</p></div><div className="form-grid"><label><span>Full name</span><input name="name" autoComplete="name" required minLength={2}/></label><label><span>Work email</span><input name="email" type="email" autoComplete="email" required/></label><label><span>Organisation</span><input name="organization"/></label><label><span>Your role</span><select name="role"><option>Healthcare professional</option><option>Hospital administrator</option><option>Researcher</option><option>Distributor or partner</option><option>Other</option></select></label><label className="form-full"><span>Password</span><input name="password" type="password" autoComplete="new-password" required minLength={10}/><small>10+ characters with upper-case, lower-case and a number.</small></label><label className="form-full"><span>Confirm password</span><input name="confirmPassword" type="password" autoComplete="new-password" required/></label></div>{error&&<p className="form-error" role="alert">{error}</p>}<button className="button button-primary button-large full-button" disabled={busy}>{busy?"Creating account…":"Create account →"}</button></form>}</div>}
